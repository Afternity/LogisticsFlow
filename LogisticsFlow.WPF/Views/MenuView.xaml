<Window x:Class="LogisticsFlow.WPF.Views.MenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogisticsFlow.WPF.Views"
        xmlns:converters="clr-namespace:LogisticsFlow.WPF.Common.Converters"
        mc:Ignorable="d"
        Title="Меню" Height="400" Width="250">

    <Grid
        Background="Azure">

        <StackPanel 
            VerticalAlignment="Center">

            <Button 
                Command="{Binding ProfileViewCommand}"
                Content="Профиль" 
                Margin="5"/>
            <Button 
                Command="{Binding AuthViewCommand}"
                Content="Авторизация" 
                Margin="5 0 0 20"/>

            <Button 
                Command="{Binding OrderViewCommand}"
                Content="Заказы" 
                Margin="5 0 0 20"/>

            <Button 
                Command="{Binding CargoViewCommand}"
                Content="Грузы" 
                Margin="5"/>
            <Button 
                Command="{Binding ClientViewCommand}" 
                Content="Клиенты" 
                Margin="5"/>
            <Button 
                Command="{Binding DriverViewCommand}"
                Content="Водители" 
                Margin="5"/>
            <Button 
                Command="{Binding EmployeeViewCommand}"
                Content="Работники" 
                Margin="5"/>
            <Button 
                Command="{Binding RouteViewCommand}"
                Content="Маршруты" 
                Margin="5"/>
            <Button 
                Command="{Binding VehicleViewCommand}"
                Content="Транспорт" 
                Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
