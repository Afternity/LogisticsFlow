<Window x:Class="LogisticsFlow.WPF.Views.ClientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogisticsFlow.WPF.Views"
        xmlns:converters="clr-namespace:LogisticsFlow.WPF.Common.Converters"
        mc:Ignorable="d"
        Title="Клиенты" Height="700" Width="1200">

    <Window.Resources>
        <converters:MessageStateToBrushConverter x:Key="MessageStateToBrushConverter"/>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="0.5*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Button
            Command="{Binding MenuViewCommand}"
            Content="Меню"
            Grid.Column="1"
            Grid.Row="0"
            HorizontalAlignment="Right"
            Margin="5"/>

            <StackPanel
            Margin="5"
            Grid.Row="1"
            Grid.Column="0">

                <TextBlock
                Text="Название:"/>
                <TextBox 
                Text="{Binding ClientAdd.Name}"
                Margin="5"/>

                <TextBlock 
                Text="Email:"/>
                <TextBox 
                Text="{Binding ClientAdd.Email}" 
                Margin="5"/>

                <TextBlock 
                Text="Телефон:"/>
                <TextBox 
                Text="{Binding ClientAdd.PhoneNumber}" 
                Margin="5"/>

                <TextBlock 
                Text="Адрес:"/>
                <TextBox 
                Text="{Binding ClientAdd.Address}" 
                Margin="5"/>

                <Button 
                Content="Создать"
                Command="{Binding CreateCommand}"
                Width="150"
                Margin="10"/>
            </StackPanel>

            <StackPanel
            Grid.Row="1"
            Grid.Column="1"
            Margin="5">

                <TextBlock
                Text="Id:"/>
                <TextBox 
                Text="{Binding ClientWorker.Id}"
                IsReadOnly="True"
                Margin="5"/>

                <TextBlock
                Text="Название:"/>
                <TextBox 
                Text="{Binding ClientWorker.Name}"
                Margin="5"/>

                <TextBlock 
                Text="Email:"/>
                <TextBox 
                Text="{Binding ClientWorker.Email}" 
                Margin="5"/>

                <TextBlock 
                Text="Телефон:"/>
                <TextBox 
                Text="{Binding ClientWorker.PhoneNumber}" 
                Margin="5"/>

                <TextBlock 
                Text="Адрес:"/>
                <TextBox 
                Text="{Binding ClientWorker.Address}" 
                Margin="5"/>

                <StackPanel
                Orientation="Horizontal"
                HorizontalAlignment="Center">

                    <Button
                    Command="{Binding UpdateCommand}" 
                    Content="Обновить"
                    Width="150"
                    Margin="10"/>
                    <Button
                    Command="{Binding DeleteCommand}"
                    Content="Удалить"
                    Width="150"
                    Margin="10"/>
                </StackPanel>
            </StackPanel>

            <StackPanel
            Grid.Row="2"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Background="AliceBlue">

                <Button 
                 Command="{Binding GetAllCommand}"
                Content="Показать все записи"
                Width="150"
                Margin="10"/>

                <ListBox
                ItemsSource="{Binding ClientList}"
                SelectedItem="{Binding ClientWorker}"
                Background="LightGray"
                Margin="10"
                Height="450">

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock
                                Text="Название:"/>
                                <TextBlock 
                                Text="{Binding Name}"
                                FontWeight="Bold"
                                Margin="5 0 0 0"
                                Width="150"/>

                                <TextBlock
                                Text="Email:"
                                Margin="5 0 0 0"/>
                                <TextBlock 
                                Text="{Binding Email}" 
                                Width="150"
                                FontWeight="Bold"
                                Margin="5 0 0 0"/>

                                <TextBlock 
                                Text="Телефон:"
                                Margin="5 0 0 0"/>
                                <TextBlock 
                                Text="{Binding PhoneNumber}"
                                Width="120"
                                FontWeight="Bold"
                                Margin="5 0 0 0"/>

                                <TextBlock 
                                Text="Адрес:"
                                Margin="5 0 0 0"/>
                                <TextBlock 
                                Text="{Binding Address}"
                                Width="200"
                                TextWrapping="Wrap"
                                FontWeight="Bold"
                                Margin="5 0 0 0"/>

                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock 
                Text="{Binding Message.Message}"
                Foreground="{Binding Message.State,
                     Converter={StaticResource MessageStateToBrushConverter}}"
                FontWeight="Bold"
                Margin="5"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
